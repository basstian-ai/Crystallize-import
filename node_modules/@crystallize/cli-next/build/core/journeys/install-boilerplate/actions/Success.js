import { Box, Newline, Text } from 'ink';
import React, { useEffect, useState } from 'react';
import { colors } from '../../../../config/colors.js';
import { Markdown } from '../../../components/Markdown.js';
import { Spinner } from '../../../components/Spinner.js';
import { useJourney } from '../context/provider.js';
export const Success = () => {
    const { state } = useJourney();
    const [showSpinnerWaiter, setShowSpinnerWaiter] = useState(true);
    useEffect(() => {
        const timeout = setTimeout(() => {
            setShowSpinnerWaiter(false);
        }, 1500);
        return () => {
            clearTimeout(timeout);
        };
    }, []);
    return (React.createElement(React.Fragment, null,
        React.createElement(Newline, null),
        React.createElement(Box, { flexDirection: "column", borderColor: colors.info, borderStyle: 'bold', alignItems: "center", width: 100, marginLeft: 2 },
            React.createElement(Text, { bold: true }, "Go fast and prosper."),
            React.createElement(Text, { dimColor: true }, "The milliseconds are with you!"),
            showSpinnerWaiter ? React.createElement(Spinner, { name: "aesthetic" }) : React.createElement(Text, null, "\u25B0\u25B0\u25B0\u25B0\u25B0\u25B0\u25B0"),
            React.createElement(Box, { flexDirection: "row", width: 90 },
                React.createElement(Markdown, null, state.readme),
                React.createElement(Box, { alignItems: "center", alignSelf: "center" },
                    React.createElement(Text, { bold: true }, '<---  '),
                    showSpinnerWaiter ? React.createElement(Spinner, { name: "runner" }) : React.createElement(Text, null, "\uD83C\uDFC3\u200D\u2642\uFE0F"))),
            showSpinnerWaiter ? React.createElement(Spinner, { name: "fistBump" }) : React.createElement(Text, null, "\uD83E\uDD1C\u2728\uD83E\uDD1B"),
            React.createElement(Newline, null))));
};
