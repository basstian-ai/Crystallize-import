import { Text } from 'ink';
import React from 'react';
import { boilerplates } from '../../../../config/boilerplates.js';
import { colors } from '../../../../config/colors.js';
import { BoilerplateChoice } from '../../../components/BoilerplateChoice.js';
import { Select } from '../../../components/Select.js';
import { useJourney } from '../context/provider.js';
export const SelectBoilerplate = () => {
    const { state, dispatch } = useJourney();
    return (React.createElement(React.Fragment, null,
        !state.boilerplate && React.createElement(Text, null, "Please select a boilerplate for your project"),
        !state.boilerplate && (React.createElement(Select, { options: boilerplates.map((boilerplate) => {
                return {
                    label: boilerplate.name,
                    value: boilerplate,
                    render: () => React.createElement(BoilerplateChoice, { boilerplate: boilerplate }),
                };
            }), onSelect: (boilerplate) => {
                dispatch.setBoilerplate(boilerplate);
            } })),
        state.boilerplate && (React.createElement(Text, null,
            "You are going to install the ",
            React.createElement(Text, { color: colors.highlight }, state.boilerplate.name),
            ' ',
            "boilerplate."))));
};
