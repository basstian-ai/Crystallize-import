import React from 'react';
type Option<T> = {
    label: string;
    value: T;
    render?: () => JSX.Element;
};
type Styles = {
    compact?: boolean;
};
export type Props<T> = {
    options: Option<T>[];
    onSelect: (value: T) => void;
    styles?: Styles;
    defaultSelectedIndex?: number;
};
export declare function Select<T>({ options, onSelect, styles, defaultSelectedIndex }: Props<T>): React.JSX.Element;
export {};
