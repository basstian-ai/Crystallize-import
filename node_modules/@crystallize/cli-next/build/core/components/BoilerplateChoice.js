import { Newline, Text } from 'ink';
import Link from 'ink-link';
import React from 'react';
export const BoilerplateChoice = ({ boilerplate }) => {
    return (React.createElement(React.Fragment, null,
        React.createElement(Text, null, boilerplate.name),
        React.createElement(Newline, null),
        React.createElement(Text, { dimColor: true }, boilerplate.baseline),
        React.createElement(Newline, null),
        React.createElement(Text, { dimColor: true }, boilerplate.description),
        React.createElement(Newline, null),
        React.createElement(Text, { dimColor: true },
            React.createElement(Link, { url: boilerplate.demo },
                React.createElement(Text, { color: "cyan" },
                    "Demo: ",
                    boilerplate.demo)))));
};
