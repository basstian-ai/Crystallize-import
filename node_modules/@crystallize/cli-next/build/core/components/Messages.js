import { Box, Newline, Text } from 'ink';
import { colors } from '../../config/colors.js';
import React from 'react';
export const Messages = ({ title = 'Note', messages }) => {
    if (messages.length === 0) {
        return null;
    }
    return (React.createElement(React.Fragment, null,
        React.createElement(Newline, null),
        React.createElement(Text, { dimColor: true },
            title,
            messages.length > 1 ? 's' : '',
            ":"),
        React.createElement(Box, null,
            React.createElement(Text, { italic: true }, messages.map((message, index) => {
                return (React.createElement(Text, { key: index },
                    React.createElement(Text, { dimColor: true, color: colors.warning }, '> '),
                    message,
                    React.createElement(Newline, null)));
            })))));
};
