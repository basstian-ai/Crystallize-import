import { ClientInterface } from './client';
import { Customer } from '../types/customer';
type Deps = {
    apiClient: ClientInterface;
};
export declare const placeCart: (cartId: string, { apiClient }: Deps, extraQuery?: any) => Promise<any>;
export declare const addSkuItem: (cartId: string, sku: string, quantity: number, { apiClient }: Deps, extraQuery?: any) => Promise<any>;
export declare const removeCartItem: (cartId: string, sku: string, quantity: number, { apiClient }: Deps, extraQuery?: any) => Promise<any>;
export declare const setCartMeta: (cartId: string, meta: Array<{
    key: string;
    value: string;
}>, merge: Boolean, { apiClient }: Deps, extraQuery?: any) => Promise<any>;
export declare const setCartCustomer: (cartId: string, customer: Customer, isGuest: boolean, { apiClient }: Deps, extraQuery?: any) => Promise<any>;
export {};
