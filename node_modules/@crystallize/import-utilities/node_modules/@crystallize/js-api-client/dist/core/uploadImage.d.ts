import * as fs from 'fs';
import { ClientInterface } from './client';
type ImageInputWithReferenceId = {
    id: string;
    filename: string;
    mimeType: string;
};
type UploadHandler = ImageInputWithReferenceId & {
    buffer: Buffer;
    stats: fs.Stats;
    apiClient: ClientInterface;
};
export declare function uploadToTenant({ id, mimeType, filename, buffer, stats, apiClient, }: UploadHandler): Promise<string | false>;
export declare function handleImageUpload(imagePath: string, apiClient: ClientInterface, tenantId?: string): Promise<string | boolean>;
export {};
