import { DocumentNode } from 'graphql';
import { ComponentInput } from '../generated/graphql';
type UpdateProps = {
    productId: string;
    sku: string;
    language: string;
    input: ComponentInput;
};
export declare function buildUpdateVariantComponentQueryAndVariables(variables: UpdateProps): {
    query: DocumentNode;
    variables: Record<string, any>;
};
export {};
