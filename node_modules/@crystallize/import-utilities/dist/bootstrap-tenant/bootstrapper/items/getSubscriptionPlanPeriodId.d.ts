import { BootstrapperContext } from '../utils';
export declare function getSubscriptionPlanPeriodId({ planIdentifier, periodName, context, }: {
    planIdentifier: string;
    periodName: string;
    context: BootstrapperContext;
}): null | string;
