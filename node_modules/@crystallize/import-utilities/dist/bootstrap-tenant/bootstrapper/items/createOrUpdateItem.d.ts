import { JSONItem } from '../../json-spec';
import { AreaUpdate, BootstrapperContext } from '../utils';
export declare function createOrUpdateItem(item: JSONItem, parentId: string, treePosition: number, onUpdate: (t: AreaUpdate) => any, context: BootstrapperContext, rootItemId: string): Promise<string | null>;
