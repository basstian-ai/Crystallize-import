import { JSONItem } from '../../json-spec';
import { AreaUpdate, BootstrapperContext } from '../utils';
export declare function handleItem(item: JSONItem, index: number, rootItemId: string, context: BootstrapperContext, onUpdate: (t: AreaUpdate) => any, finishedItems: number, totalItems: number): Promise<number>;
