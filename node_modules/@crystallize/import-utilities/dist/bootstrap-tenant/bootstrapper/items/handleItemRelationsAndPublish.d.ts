import { JSONItem } from '../../json-spec';
import { BootstrapperContext } from '../utils';
export declare function handleItemRelationsAndPublish(item: JSONItem, context: BootstrapperContext, onUpdate: any, finishedItems: number, totalItems: number): Promise<number>;
