import { BootstrapperContext } from '../utils';
export declare function publishItem(language: string, id: string, context: BootstrapperContext): Promise<void> | Promise<import("../utils").IcallAPIResult>;
