import { BootstrapperContext } from '.';
import { ProductVariant } from '../../../generated/graphql';
type ExistingProduct = {
    id: string;
    vatType: {
        name: string;
    };
    variants: ProductVariant[];
};
export declare function getProduct(language: string, itemId: string, context: BootstrapperContext): Promise<ExistingProduct>;
export {};
