"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.KillableWorker = void 0;
class KillableWorker {
    constructor() {
        this.isKilled = false;
        this.isPaused = false;
        this._workIntervalId = null;
        this._workPausedTimeout = null;
    }
    kill() {
        if (this._workIntervalId) {
            clearTimeout(this._workIntervalId);
        }
        if (this._workPausedTimeout) {
            clearTimeout(this._workPausedTimeout);
        }
        this.isKilled = true;
    }
    pauseFor(ms, cb) {
        if (this._workPausedTimeout) {
            clearTimeout(this._workPausedTimeout);
        }
        this._workPausedTimeout = setTimeout(() => {
            this.isPaused = false;
            cb();
        }, ms);
        this.isPaused = true;
    }
}
exports.KillableWorker = KillableWorker;
