import { BootstrapperContext } from '.';
import { JSONGrid } from '../../json-spec';
type GetAllGridsOptions = {
    setItemExternalReference: boolean;
};
export declare function getAllGrids(language: string, context: BootstrapperContext, options?: GetAllGridsOptions): Promise<JSONGrid[]>;
export {};
